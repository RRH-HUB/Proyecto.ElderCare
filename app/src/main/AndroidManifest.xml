<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.proyecto.eldercare">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddDishDietaryActivity"></activity>
        <activity android:name=".DietaryActivity" />
        <activity android:name=".SOSActivity" />
        <activity android:name=".AddEntryDiaryActivity" />
        <activity android:name=".DiaryActivity" />
        <activity android:name=".AddPillsActivity" />
        <activity android:name=".PillsActivity" />
        <activity android:name=".AddCalendarActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".PersonActivity" />
        <activity android:name=".AddPersonActiivity" />
        <activity android:name=".StartActivity" />
        <activity android:name=".LoginUserActivity" />
        <activity android:name=".RegisterUserActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>